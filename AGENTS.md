# AI-Native 项目交接文档（Agent Onboarding）

> 本文档是给新对话中的 AI agent 的"交接手册"。请在开始任何工作前完整阅读本文件，以便快速理解项目背景、当前进度与后续计划。
>
> 仓库地址：https://github.com/Twilight-aa/AI-Native.git
> 本地路径：D:\Programs\Games
> 主分支：main

---

## 1. 新 Agent 上手流程（必读）

按以下顺序执行，确保不在信息缺失的情况下开工：

1. **通读本文档**：重点关注「3. 当前状态」「4. 已知不足」「5. 后续工作计划」。
2. **同步仓库**：运行 `git status` 和 `git log --oneline -10`，确认本地与 `origin/main` 一致、工作区干净。
3. **确认任务目标**：与用户确认本次对话要完成的具体任务，并与「后续工作计划」对照，确认优先级。
4. **开工前检查**：遵循第 6 节的开发约定；不要擅自提交代码，除非用户明确要求。
5. **收尾时更新文档**：任何实质性进展完成后，必须按第 7 节的规范更新本文档。

---

## 2. 项目概述

- **仓库名称**：AI-Native
- **游戏暂定名**：《风邮局》（英文名待定）
- **项目定位**：在 10 天内从零制作并提交一款横屏 H5 单机休闲经营游戏。玩家经营山顶邮局，为具有奇异属性的包裹选择包装方式、分配邮鸟并规划投递路线。
- **核心卖点**：包装决策会直接改变路线风险与运输结果，即“包装决定旅途”。
- **目标体验**：容易上手、单关约 3～5 分钟、具有轻度经营调度和策略取舍，完整提供至少 50 关及明确的难度递进。
- **视觉方向**：英式水彩绘本风；米白纸张底纹、深棕铅笔轮廓、低饱和蓝/绿/橙色，UI 使用邮票、信封和手绘地图语言。
- **计划技术栈**：Phaser + TypeScript + Vite，使用 JSON/TypeScript 数据配置关卡，`localStorage` 保存本地进度，静态 H5 构建，无后端。
- **目标分辨率**：横屏 `1280 × 720` 固定逻辑分辨率，通过 `Phaser.Scale.FIT` 和 `CENTER_BOTH` 适配浏览器。

### 比赛硬性约束（来自用户提供的比赛要求截图）

- 制作可在主流平台体验的休闲游戏，本项目交付 H5 版本。
- 可采用已验证玩法并加入微创新，也允许完全原创。
- 使用 AI 辅助从创意到成品的制作过程。
- 至少设计 50 关，体现难度递进和体验变化。
- 评审重点是玩法与关卡，不需要复杂美术或故事。

> Day 2 纵向切片及其规则修复已经完成。后续 agent 不应继续无边界扩充创意，应按第 11 节进入 Day 3 核心规则扩展。

---

## 3. 当前状态（截至最近一次更新）

**最近更新日期**：2026-08-21

### 已完成
| 日期 | 事项 | 提交 |
|---|---|---|
| 2026-08-21 | 初始化本地 Git 仓库（`git init`，默认分支 main） | — |
| 2026-08-21 | 添加 `.gitignore`（依赖、构建产物、密钥、日志、编辑器/系统文件） | `b3074b4` |
| 2026-08-21 | 关联远程仓库并推送 main 分支 | `b3074b4` |
| 2026-08-21 | 创建本交接文档 | 待提交 |
| 2026-08-21 | 确认游戏方向为《风邮局》，完成核心玩法、技术架构、50 关难度结构及 10 天交付计划 | 待提交 |
| 2026-08-21 | Day 1：搭建 Phaser + TS + Vite 工程，配置 typecheck/build/lint/test，写 6 个场景占位，跑通"开始→第 1 关→结算"完整闭环 | `9a25269` |
| 2026-08-21 | Day 2：完成纵向切片——怕水包裹"鸟鸣"、纸盒/蜡纸包装、晴/雨路线、确定性结算规则(evaluatePacking/evaluateRoute/evaluateDelivery)及 8 个自动测试；PostOffice/Route/Result 场景替换为真实逻辑 | `5dbbdc9` |
| 2026-08-21 | Day 2 复验修复：强制容器与槽位互斥、修正透气/路线特性/准时/巧思结算、扩展材料与路线网格、多特性显示；自动测试增至 14 条 | 待提交 |

### 当前进度总结
Day 2 纵向切片及复验修复已完成：核心路径可运行，包装必须包含容器，同槽位互斥，透气/路线/准时/巧思结算为确定性规则；14 个自动测试、typecheck、lint 和生产构建均通过。下一步进入 Day 3。

---

## 4. 已知不足 / 待解决问题

1. **比赛细则待核对**：正式提交入口、包体限制、素材与 AI 使用声明格式、截止时间及浏览器兼容要求仍需从比赛官方信息确认。
2. **关卡尚未制作**：除第 1 关外，50 关的数据、教学顺序、数值和试玩验证均未开始。
3. **核心规则尚未完整实现**：已有"怕水"和"需要透气"，但木箱、羽毛垫、易碎、怕光、强光、颠簸、邮鸟负载等 Day 3 内容尚未接入真实关卡。
4. **人工交互验收待补**：自动测试和构建通过，但本轮浏览器连接失败，鼠标/触屏真实点击流程仍需补测。
5. **美术与音频资产缺失**：需要统一生成/制作、筛选、压缩并记录来源和授权情况。
6. **无 README**：仓库缺少面向人类的 README。
7. **无 CI/CD**：十天赛程内优先级较低，只有核心成品稳定后再配置。
8. **存在未提交修改**：Day 2 复验修复及本次文档更新尚未提交；未经用户明确要求不得 commit/push。

---

## 5. 后续工作计划

按优先级排列：

- [x] P0：确认项目目标、核心玩法、比赛版范围与计划技术栈
- [ ] P0：核对比赛官方提交细则和确切截止时间
- [x] P0：搭建 Phaser + TypeScript + Vite 工程及 lint/typecheck/build/test 基础命令
- [x] P0：在 24 小时内完成“怕水包裹 → 蜡纸包装 → 雨路成功/未防水失败”的纵向切片
- [ ] P0：完成 5 种包裹特性、4 种包装材料、3 种路线危险的核心规则
- [ ] P0：完成关卡解锁、三星评价、本地存档和 50 关配置
- [ ] P0：完成全 50 关可通关性、移动端和生产构建测试
- [ ] P0：准备最终 H5 包、封面、截图、演示视频、作品说明和 AI/素材使用记录
- [ ] P1：建立并替换统一的英式水彩美术和基础音效
- [ ] P1：编写 README.md，并根据实际工程细化 `.gitignore`
- [ ] P2：仅在核心成品稳定且时间充足时配置 CI

---

## 6. 开发约定

- **分支**：当前直接使用 `main` 分支开发；如需分支策略请与用户确认。
- **提交**：未经用户明确要求，不得执行 commit / push。提交信息使用简洁的约定式风格（如 `chore:`、`feat:`、`fix:`）。
- **语言**：与用户交流使用中文。
- **环境**：Windows PowerShell（5.1），注意 `&&` 不可用，使用 `cmd1; if ($?) { cmd2 }` 串联命令。
- **代码风格**：使用 TypeScript；不主动添加无价值注释；核心规则优先写成纯函数并配测试；关卡不得复制场景代码，必须数据驱动。
- **十天赛程原则**：每天结束必须保留一个可运行版本；先灰盒后美术；先完整闭环后增加内容；第 8 天后原则上禁止增加新系统。
- **范围控制**：除非用户明确改变方向，否则不得自行加入后端、登录、联网、多人、React/Vue、自由移动角色、复杂物理装箱、商店装修、技能树或复杂剧情。
- **验证要求**：每项实现至少执行与风险相称的 typecheck/test/build；最终提交前必须从干净环境验证生产构建和全部 50 关。

---

## 7. 文档维护规范（重要）

本文档是跨对话的记忆载体，每位 agent 都有义务维护它：

1. **何时更新**：完成任何实质性工作（新增功能、修复问题、架构决策、发现新缺陷）后。
2. **如何更新**：
   - 在「3. 当前状态 → 已完成」表格中追加一行（日期、事项、提交号）。
   - 更新「当前进度总结」一句话概括。
   - 新发现的问题写入「4. 已知不足」；完成的工作从「5. 后续工作计划」勾选。
   - 重大架构/技术决策，在下方「8. 决策记录」追加条目。
3. **原则**：保持客观、简洁、可执行；避免流水账，突出"新 agent 必须知道的事"。

---

## 8. 决策记录（ADR 简表）

| 日期 | 决策 | 原因 |
|---|---|---|
| 2026-08-21 | 用 AGENTS.md 作为交接文档 | opencode 会自动读取该文件，新对话可零成本获取上下文 |
| 2026-08-21 | 采用《风邮局》而非厨房经营题材 | 保留限时订单和多工序调度的通用乐趣，同时以包装、邮鸟和路线形成独立身份，降低同质化风险 |
| 2026-08-21 | 以“包裹—包装—路线”三角关系作为唯一核心系统 | 包装可弥补路线风险、路线可弥补包装不足、时限迫使玩家取舍，机制之间存在可解释的因果关系 |
| 2026-08-21 | 比赛版采用双阶段单因果链 | 邮局包装阶段的状态直接传递到路线与运输阶段，避免两个小游戏相互割裂 |
| 2026-08-21 | 装箱简化为容器/外层/填充槽位，不做复杂俄罗斯方块物理 | 保留包装决策，降低十天内的实现成本、触屏故障率和关卡验证量 |
| 2026-08-21 | 使用 Phaser + TypeScript + Vite 的纯 2D 静态 H5 架构 | 与比赛交付形式匹配、工具链轻量、无需后端，便于快速构建与部署 |
| 2026-08-21 | 50 关使用数据配置和规则重组实现 | 控制开发成本，并允许统一校验、批量生成初稿和人工调整难度曲线 |
| 2026-08-21 | 第 8 天起停止增加玩法系统 | 为完整试玩、修复、打包和提交预留必要时间 |
| 2026-08-21 | Day 1：6 场景全占位提前锁定架构 | 按 §10.3 一次建好所有场景，避免后期重构，Day 2 直接填业务逻辑 |
| 2026-08-21 | 包装槽位互斥且必须包含容器 | 防止只用蜡纸等外层材料完成投递，保证包装模型与玩家认知一致 |
| 2026-08-21 | 整体透气性取所选材料最低值 | 外层密封会限制整个包装，不能通过简单加法让叠层包装变得更透气 |
| 2026-08-21 | 路线危险只伤害具有对应敏感特性的包裹 | 保持包裹特性、包装决策和路线风险之间的可解释因果关系 |

---

## 9. 游戏设计架构（已确认）

### 9.1 核心循环

```text
接收委托
  → 识别包裹特性
  → 选择容器、外层和填充材料
  → 称重并交给邮鸟
  → 选择/绘制投递路线
  → 自动运输并结算
  → 获得星级、解锁下一关
```

- 单关目标时长：约 3～5 分钟。
- 基础输入：点击、拖拽、划线；必须同时兼容鼠标和触摸。
- 关内循环：订单判断、包装、路线规划、运输结算。
- 关外循环：关卡解锁、星级记录、章节推进；比赛版不做复杂养成和数值碾压。

### 9.2 比赛版内容上限（范围冻结）

#### 包裹特性（5 种）

| 特性 | 核心要求 |
|---|---|
| 易碎 | 需要足够防震 |
| 怕水 | 需要足够防水或避开雨区 |
| 怕光 | 需要遮光或避开强光路线 |
| 需要透气 | 不能被完全密封 |
| 限时 | 运输时间不能超过订单期限 |

#### 包装材料（4 种）

| 材料 | 优势 | 代价 |
|---|---|---|
| 纸盒 | 重量低 | 防护弱、怕水 |
| 木箱 | 防震高 | 重量高 |
| 蜡纸 | 防水 | 降低透气性 |
| 羽毛垫 | 防震、较轻 | 占用填充槽/增加少量重量 |

#### 路线危险（3 种）

- 雨区：产生防水需求。
- 强光：产生遮光需求。
- 颠簸气流：产生防震需求。

#### 邮鸟（最多 2 只）

- 轻快型：速度快、负载低。
- 负重型：速度慢、负载高。

### 9.3 核心因果与结算

系统只保留容易解释的确定性判断，第一版禁止加入随机成功率：

```text
包装防水值 >= 路线防水需求
包装防震值 >= 路线防震需求
包装遮光值 >= 路线遮光需求
需要透气的包裹未被完全密封
总重量 <= 邮鸟负载
运输时间 <= 订单时限
```

失败必须明确指出原因，例如“未防水而经过雨区”“防震不足”“包裹无法呼吸”“邮鸟超重”或“超时”。玩家应能理解更换哪项包装或路线可以成功。

### 9.4 单关阶段

1. **邮局包装阶段**：读取订单；在容器、外层、填充三个槽位中做选择；查看重量和保护属性；确认发件。
2. **路线规划阶段**：把包裹分配给邮鸟；查看不同路线的距离和危险；选择或绘制路线。
3. **运输结算阶段**：播放约十几秒自动飞行动画；根据确定性规则展示逐项结果和失败原因。

比赛版不做自由移动角色。邮局操作采用直接点击工作台/卡片的方式；若进度落后，路线绘制也应降级为清晰的路线二选一。

### 9.5 计分

- **安全星**：包裹完好抵达。
- **准时星**：在时限内抵达。
- **巧思星**：节省材料、控制重量或满足本关指定条件。

星级不应只由手速决定，使休闲玩家也能通过规划获得高分。

### 9.6 50 关难度曲线

| 章节 | 关卡 | 教学/变化重点 |
|---|---:|---|
| 晴风丘陵 | 1～10 | 单一规则、基础包装、重量和路线二选一 |
| 细雨港湾 | 11～20 | 防水与透气冲突、多包裹初步组合 |
| 钟声山谷 | 21～30 | 时限、负载、邮鸟差异和多目的地 |
| 迷雾群岛 | 31～40 | 多条件组合、有限路线信息或天气变化 |
| 天空邮路 | 41～50 | 综合挑战；第 50 关为全部已教学规则的最终考试 |

设计原则：每 5 关引入或强化一个变量，每 10 关进行一次综合考试；不可仅靠不断缩短倒计时制造难度。先手工设计 10 个原型，再制作难度变体并人工试玩、排序。AI 可生成配置初稿，但不得把未试玩的关卡直接视为完成。

---

## 10. 实现架构

### 10.1 建议目录

```text
src/
├─ main.ts
├─ scenes/
│  ├─ BootScene.ts
│  ├─ MenuScene.ts
│  ├─ LevelSelectScene.ts
│  ├─ PostOfficeScene.ts
│  ├─ RouteScene.ts
│  └─ ResultScene.ts
├─ systems/
│  ├─ OrderSystem.ts
│  ├─ DeliverySystem.ts
│  └─ SaveSystem.ts
├─ data/
│  ├─ materials.ts
│  ├─ parcels.ts
│  └─ levels.ts
├─ ui/
│  ├─ Button.ts
│  └─ TutorialOverlay.ts
└─ types/
   └─ game.ts

public/assets/
├─ images/
├─ audio/
└─ fonts/
```

允许在实际实现中合并小文件，禁止为了“架构完整”而在纵向切片之前过度抽象。

### 10.2 数据驱动要求

关卡最少应配置：`id`、章节、时限、订单、可用材料、路线、邮鸟和三星条件。订单由收件人、货物、1～2 个特性、期限、目的地和可选特殊要求组合。所有 50 关必须复用同一套场景与规则系统。

建议建立纯函数：

```text
evaluatePacking(parcel, selectedMaterials)
evaluateRoute(route, bird, packingResult)
evaluateDelivery(order, packingResult, routeResult)
```

为这些函数编写表驱动测试，至少覆盖每种属性成功/失败、边界重量、边界时限以及“需要透气 + 蜡纸”的冲突。

### 10.3 场景与状态流

```text
BootScene
  → MenuScene
  → LevelSelectScene
  → PostOfficeScene
  → RouteScene
  → ResultScene
  → 下一关或重试
```

关卡状态在包装与路线场景之间显式传递，禁止依赖散落的全局可变变量。`SaveSystem` 只保存已解锁关卡、各关最高星级、音量设置和必要版本号。

### 10.4 美术和音频生产

- 每章复用 1 张主背景，通过天气、前景和色调变化制造差异，不为 50 关单独画背景。
- 优先制作会影响理解的资产：包裹、材料、危险、邮鸟、属性图标和按钮；装饰最后处理。
- 花体仅用于标题，正文和属性必须清晰易读。
- 重要状态同时使用颜色、图标、动画或文字，不能仅依赖颜色。
- 音频上限：1 首循环背景音乐及约 6～8 个短音效，提供音乐/音效开关。
- 所有 AI 生成或第三方素材必须记录来源、生成方式、授权状态和修改过程，以便提交说明。

---

## 11. 十天交付计划

| 天数 | 必须完成的可验收成果 |
|---:|---|
| Day 1 | 初始化工程、缩放、场景切换、临时开始页/选关页；跑通“开始→第 1 关→结算”占位闭环；配置 typecheck/build |
| Day 2 | 完成首个纵向切片：“怕水包裹→选择纸盒/蜡纸→晴/雨路线→明确成功或失败→重试/下一关” |
| Day 3 | 实现 5 种特性、4 种材料、3 种危险、重量/时限/负载和确定性结算；至少 5 个测试关卡 |
| Day 4 | 完成鼠标/触摸操作、选择/撤销/重置、状态预览和错误反馈；不增加新规则 |
| Day 5 | 完成首章 10 关、新手引导、解锁、三星和本地存档；此时形成可独立提交的 10 关安全版本 |
| Day 6 | 以数据驱动方式扩展到 5 章 50 关，完成配置校验并人工调整顺序 |
| Day 7 | 替换正式英式水彩美术、基础动画、背景音乐和音效；完成素材记录 |
| Day 8 | 从第 1 关连续测试到第 50 关，修复阻断问题；测试 Chrome、Edge、触屏/移动模拟、缩放、刷新和存档；停止增加新系统 |
| Day 9 | 打磨首屏、前三关、第 10/30/50 关、加载和反馈；压缩资产；准备作品说明、截图、封面和 30～60 秒演示视频 |
| Day 10 | 只做干净构建、解压复验、最终全流程测试、上传及上传后在线复验；保存最终包和提交成功截图 |

每天结束都要留下可运行构建，并更新本文档实际进度。若任一天的核心验收未完成，下一天优先补齐，不得用新增创意掩盖未闭环的基础功能。

---

## 12. 砍功能顺序与不可删除项

### 进度落后时按此顺序删除

1. 邮票收藏册。
2. 第二只邮鸟。
3. 额外挑战目标。
4. 自动运输中的一次性援助操作。
5. 复杂包装摆放动画。
6. 章节过场和剧情对话。
7. 自由绘制路线，改为路线二选一。
8. 多订单同时处理，改为顺序订单。

### 不可删除

- “包装选择会改变路线结果”的核心因果关系。
- 清晰的成功/失败解释。
- 至少 50 个可进入、可完成并有难度递进的关卡。
- 前三关新手引导。
- 关卡解锁和本地存档。
- 基础音效与统一视觉。
- 最终生产构建和完整提交复验。

### 下一位 agent 的第一任务

先核对比赛官方提交细则，并补做 Day 2 真实浏览器点击验收；随后进入 Day 3：接入木箱、羽毛垫、易碎、怕光、强光、颠簸、重量/邮鸟负载和至少 5 个测试关卡。除非用户明确要求，不要先制作大量美术。
